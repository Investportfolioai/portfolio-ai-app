import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Documents — Portfolio AI" };

export default function DocumentsPage() {
  return (
    <PagePlaceholder
      title="Documents"
      description="Deal files, term sheets, and signed agreements."
    />
  );
}
