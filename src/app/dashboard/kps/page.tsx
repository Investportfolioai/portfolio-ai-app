import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Key Principals — Portfolio AI" };

export default function KeyPrincipalsPage() {
  return (
    <PagePlaceholder
      title="Key Principals"
      description="KP roster, balance sheets, and deal attachments."
    />
  );
}
