import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Deals — Portfolio AI" };

export default function DealsPage() {
  return (
    <PagePlaceholder
      title="Deals"
      description="Every deal in the book, beyond the pipeline view."
    />
  );
}
